name: Deploy to VPS

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            # 1. Navegar al directorio y asegurar permisos
            cd ~/BlindCheck || exit 1
            sudo chown -R $USER:$USER ~/BlindCheck

            # 2. Detener servicios externos que chocan con puertos 80/443
            echo "Stopping conflicting services..."
            sudo systemctl stop nginx 2>/dev/null || true
            sudo systemctl stop apache2 2>/dev/null || true

            # 3. Actualizar código (Limpia cambios locales accidentales)
            echo "Syncing with GitHub..."
            git fetch --all
            git reset --hard origin/main

            # 4. Limpieza Profunda de Docker (Evita que el disco se llene)
            echo "Cleaning up Docker environment..."
            docker compose down --remove-orphans
            # Borra imágenes viejas de más de 24h para liberar espacio
            docker image prune -a -f --filter "until=24h"
            # Borra caché de construcción para liberar más espacio
            docker builder prune -f



            # 5. Gestión de Certificados SSL (Manual - ver init-letsencrypt.sh)
            # El paso de generación automática se ha eliminado para evitar bloqueos de Let's Encrypt.
            # Los certificados se persisten en el servidor en ./certbot
            if [ -d "./certbot/conf/live/blindcheck.space" ]; then
               echo "✅ SSL: Certificados detectados."
            else
               echo "⚠️ SSL: No se detectaron certificados. RECUERDA: Ejecutar ./init-letsencrypt.sh manualmente en el servidor la primera vez."
            fi

            # 6. Despliegue y Re-construcción
            echo "🚀 Lanzando contenedores..."
            docker compose up -d --build

            # 7. Verificación de salud y Logs de error
            echo "Final verification..."
            sleep 10
            docker compose ps
            
            # Si el frontend no sube, nos muestra el error en los logs de GitHub
            if [ $(docker inspect -f '{{.State.Running}}' blindcheck-frontend) != "true" ]; then
              echo "❌ ERROR: El frontend falló al iniciar. Mostrando logs:"
              docker logs blindcheck-frontend
              exit 1
            fi

            # Verificación específica para el backend
            if [ $(docker inspect -f '{{.State.Running}}' blindcheck-backend) != "true" ]; then
              echo "❌ ERROR: El backend falló al iniciar. Mostrando logs:"
              docker logs blindcheck-backend
              exit 1
            fi
            
            echo "✅ Despliegue completado con éxito."